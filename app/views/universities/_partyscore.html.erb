<!-- Trigger the modal with a button -->
<button type="button" class="btn btn-info btn-lg" data-toggle="modal" data-target="#PartymyModal">New Party Score</button>
<!--  -->
<!-- Modal -->
<div class="modal fade" id="PartymyModal" role="dialog">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Your Party Score</h4>
      </div>
      <div class="modal-body">

<%= form_for [@country,@university, @party_score], remote: true, :html => {:id => 'party_review_form'} do |f| %>
  <% if @academic_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@party_score.errors.count, "error") %> prohibited this party_score from being saved:</h2>

      <ul>
      <% @party_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :party_score %><br>
    <%= f.number_field :party_score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
    </div>
  </div>
</div>

<h1>Party Score: <span id="party_reviews_count"><%= @university.average_party_score %></span></h1>


<!--  -->