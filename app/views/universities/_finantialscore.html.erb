<!-- Trigger the modal with a button -->
<button type="button" class="btn btn-info btn-lg" data-toggle="modal" data-target="#FinantialmyModal">New Finantial Score</button>
<!--  -->
<!-- Modal -->
<div class="modal fade" id="FinantialmyModal" role="dialog">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Your Finantial Score</h4>
      </div>
      <div class="modal-body">

<%= form_for [@country,@university, @finantial_score], remote: true, :html => {:id => 'finantial_review_form'} do |f| %>
  <% if @finantial_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finantial_score.errors.count, "error") %> prohibited this finantial_score from being saved:</h2>

      <ul>
      <% @finantial_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :Finantial_score %><br>
    <%= f.number_field :Finantial_score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
    </div>
  </div>
</div>

<h1>Finantial Score: <span id="finantial_reviews_count"><%= @university.average_finantial_score %></span></h1>


<!--  -->
