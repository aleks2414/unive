<%= search_form_for @q, remote: true, id: 'my_search' do |f| %>
  <%= f.label :Search_cont %>
  <%= f.search_field :name_or_country_name_or_careers_name_cont %>

  <%= f.label :Mexico %>
  <%= f.check_box :country_id, :value => 'Mexico', :id => 'MexicoP' %>

    <%#= f.select :country_id_eq, options_from_collection_for_select(Country.all, "id", "name"), include_blank: true %>


  <%= f.check_box :is_wifi_true %>
  <%= f.label :is_wifi_true %>

  <%#= f.submit "search" %>
<% end %>

<%= content_tag :table do %>
  <%= content_tag :th, sort_link(@q, :name) %>
  <%= content_tag :th, sort_link(@q, :country_name) %>
  <%= content_tag :th, sort_link(@q, :careers_name) %>
<% end %>

<div id="results">
    <%= render 'results' %>
</div>

<br>

<%= link_to 'New University', new_university_path %>


