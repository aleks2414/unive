<h1>Editing Career</h1>

<%= render 'form' %>

<%= link_to 'Show', country_university_career_path(@country, @university, @career) %> |
<%= link_to 'Back', country_university_path(@country, @university) %>
