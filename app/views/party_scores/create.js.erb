// page.insert_html :bottom, :reviews, :partial => 'review', :object => @review
// page.replace_html :reviews_count, pluralize(@review.product.reviews.size, 'Review')
// page[:review_form].reset
// page.replace_html :notice, flash[:notice]
// flash.discard
<% if @party_score.errors.empty? %>
$('.alert').html("El producto ha sido creado con éxito").show();

$("#party_reviews_count").html("<%= pluralize(@university.average_party_score,'') %>");
$("#party_review_form")[0].reset();
$('#PartymyModal').modal('hide');
<% else %>
// show first error
alert('<%= @party_score.errors.full_messages[0] %>');
<% end %>